/**
 * GNB URL ID → BE PD 분류 code/categoryId. 표시 이름으로 같은 분류임을 추정하지 않는다.
 * TODO BE가 같은 쓰임 분류 축의 ID를 확정한 뒤 이 표에 명시적으로 등록한다.
 * 현재 확정된 매핑은 없다. MSW의 kitchen·kitchen-1·예시 숫자를 여기에 넣지 않는다.
 */
export const PRODUCT_CATEGORY_API_CODES: Readonly<Record<string, string>> = {};
